// Student panel reuses the shared TeacherAdmin design-system primitives so the
// two panels stay visually consistent. Re-exported here for clean local imports.
export * from '../../TeacherAdmin/shared';
